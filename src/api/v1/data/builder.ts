

import { EntityType, Entity } from "../models";
import { SysError } from "../exceptions";

export class Builder {

    static applyDefaults(entityType: EntityType, entity: Entity) {

    }

    static buildLinkedProperty(entityType: EntityType, entity: Entity) {

    }
}